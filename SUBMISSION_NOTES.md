# 提交说明

## 仓库性质

本仓库是“第二面试官”项目的面试技术验证脱敏版。它从现有代码中抽取核心链路，删除了生产环境配置、部署记录、日志、截图、真实会议产物、真实简历和真实逐字稿。所有 `demo-data` 均为 synthetic（人工虚构）数据，不对应真实个人或组织。

## 已移除或替换

- 所有 `.env`、API Key、Token、Cookie、数据库密码、飞书凭据和账号信息；
- 内部域名、内部 IP、真实回调地址和生产数据库连接；
- 真实候选人、员工、面试官、简历、会议链接、逐字稿和面评；
- 生产 deployment、备份、migration、seed、运行日志和服务器操作文档；
- 与技术验证无关的历史交接包、截图、压缩包和构建产物。

`.env.example` 中只有不可用的占位值和本地回环地址。

## 代码能够证明的能力

- Interview Package 的严格输入校验和幂等导入；
- 以 Interview 聚合承载一场面试的事实与状态；
- 会前 Topics / Criteria / Opening Questions 的结构化生成、校验和规则回退；
- TranscriptLine 持久化、来源标识、去重、游标、租约和重试；
- 增量逐字稿触发会中追问，最小批量、间隔、退避、去重和有效期；
- SSE 接口与前端过期建议过滤；
- 会后 Review Draft 的结构化生成和规则回退；
- quote / lineId 回绑真实候选人 TranscriptLine，以及批准前的 Evidence 再校验；
- 人工编辑、人工批准和审计边界；
- Unit、Schema、Prompt 安全、LLM 传输和降级相关工程测试。

当前已执行口径为 `pnpm test:unit`：16 个测试文件、246 个 test case 通过。仓库总计保留 23 个测试文件、静态统计 305 个 `it/test` 声明；数据库 API 集成部分未在本次脱敏流程中执行，不能把 246 描述成全部集成测试，更不能描述成真实面试效果样本。

## 设计存在但尚不能证明完成的部分

- 真实飞书账号与真实会议的完整 E2E 没有在本仓库中验证；
- 实时字幕取数依赖外部权限和运行环境，脱敏版没有凭据；
- API 与 Worker 分进程时，进程内 SSE 事件总线有推送丢失风险；
- 会前 sourceRefs 的数据结构存在，但当前生成链路没有完成和会后 Evidence 同等级的原文硬绑定；
- 真实招聘系统的候选人、职位和面试安排自动同步未纳入提交版；
- Golden Dataset、历史时点回放和真实产品效果指标属于建议评测方案，尚未执行。

## Worker / API 跨进程风险

当前 `apps/api/src/routes/sse.ts` 在内存中保存订阅者。`capture-worker.ts` 和 `followup-engine.ts` 可以运行在 Worker 进程，而浏览器 SSE 连接由 API 进程持有。两个进程不共享内存，因此 Worker 调用 `publishEvent` 并不能保证 API 连接收到事件。数据库事实仍可落盘，但“立即推送”需要外部消息通道或 Outbox relay 才能可靠成立。

## 外发前人工确认

自动脱敏只能检查文本与常见凭据模式，不能判断代码知识产权归属。提交前需由项目 Owner 或公司授权人确认：核心源文件可对外展示；第三方 SDK/接口使用符合许可；虚构数据没有影射真实候选人；README 中的完成状态与实际验收记录一致。
