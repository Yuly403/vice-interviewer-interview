# 脱敏与验证报告

检查日期：2026-08-26

## 范围

只检查本提交目录中的源代码、配置示例、测试、文档和 Demo 数据。未扫描电脑其他目录，也未访问生产数据。

## 从提交版排除的源项目内容

- 生产 `.env` 与部署配置；
- 真实会议运行产物和逐字稿目录；
- 历史日志、截图、交接文档、备份、压缩包和构建产物；
- 数据库 migration、seed 与生产运维脚本；
- 内部域名、内部路径和内部基础设施信息。

## 最终模式扫描

以下高风险模式在提交目录中均为 0 命中：

- 公司内部品牌、域名和历史产品名；
- Windows 用户绝对路径和服务器用户绝对路径；
- 10/172.16–31/192.168 私网 IP；
- 常见真实 API Key 前缀和 Bearer 字面值；
- 中国大陆手机号和身份证号模式；
- 非 `example.com/.org/.net` 的邮箱字面值；
- `.env`、日志、数据库 dump、上传目录、会议产物和 zip 文件。

代码中仍然保留 `API_KEY`、`SECRET`、`TOKEN`、`COOKIE`、`PASSWORD`、`Authorization` 等**配置字段名和安全逻辑**，因为它们是认证、加密和脱敏能力的必要证据；其值只来自环境变量，测试值明确为 synthetic/test placeholder。`.env.example` 只包含不可用占位值、本地回环地址和 `example.com` 域名。

## 验证结果

- `pnpm test:unit`：16 个测试文件、246 项测试全部通过；仓库总计保留 23 个测试文件、静态统计 305 个 `it/test` 声明；
- `pnpm typecheck`：contracts、database、llm、domain、web、api 全部通过；
- Demo Interview Package 和 6 条 JSONL TranscriptLine 已通过真实 Zod Schema；
- 完整数据库集成测试未运行，因为本次没有配置专用 `_test` PostgreSQL，避免误连任何已有数据库。

## 自动检查无法替代的事项

- 源码对外披露权、公司知识产权和劳动合同保密义务；
- 第三方依赖与飞书接口使用许可；
- 虚构案例是否在语义上仍可能让内部人员联想到特定真实项目；
- README 的角色描述和完成状态是否与可出示的项目记录一致。
